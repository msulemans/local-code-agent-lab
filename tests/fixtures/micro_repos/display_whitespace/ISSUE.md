# Display names retain irregular whitespace

Display names may contain tabs or repeated spaces copied from external forms.
They should contain single spaces between words with no surrounding whitespace.
The existing simple-space examples work, but irregular whitespace does not.
